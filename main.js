import * as THREE from 'three';
import { FBXLoader } from 'three/addons/loaders/FBXLoader.js';
import * as CANNON from 'cannon-es';

// Full controller - see repo for complete code. This is a placeholder to restore; the complete version is in the local project.
console.log('Please use the full main.js from the project');
document.getElementById('hud').textContent = 'Upload bar_end.fbx and full main.js to complete';
